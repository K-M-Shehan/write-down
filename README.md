# Azure lab

> Made for a lab in a 3rd year module called "Case Study Project".

I've decided to use a mono-repo for this project so both the frontend (React) and backend will be in the same repository. The reason for using a mono-repo is because the app itself will be extremely basic.

The content itself was almost made with generative AI but I changed that decision after seeing how absymal the code it generated looked, I would rather spend some time writing the code myself rather than use that. The app made for this lab is a notes app, which would require a backend to store the notes maybe.
